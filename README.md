# Set this up to your config folder. 
 - `git init`
 - `git remote add origin git@github.com:Trevo525/cs2-autoexec.git`
 - `git fetch`
 - `git checkout -f main`

# Tell CS2 to use the autoexec file.
 - Put this in your launch options: `+exec autoexec.cfg`
